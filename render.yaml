services:
  - type: web
    name: ridecircle-api
    runtime: node
    plan: free
    buildCommand: "echo no build step needed"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: 20
